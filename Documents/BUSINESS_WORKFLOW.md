# Product Warehouse - 业务流程和数据管理方案

## 🎯 核心问题解答

### 问题 1: 订单状态如何管理？

**答案**：通过网站界面手动更新 + 自动规则

#### 订单状态流转

```
创建订单
    ↓
[pending] 待处理
    ↓ (仓库人员点击"开始处理")
[processing] 处理中
    ↓ (仓库人员点击"标记已发货")
[shipped] 已发货
    ↓ (系统自动或手动确认)
[completed] 已完成

(任何状态都可以) → [cancelled] 已取消
```

#### 实现方式

1. **后端 API**：
   ```
   PUT /api/v1/orders/{order_id}/status
   Body: { "status": "processing", "notes": "开始拣货" }
   ```

2. **前端界面**：
   - 订单列表页面：显示所有订单及其状态
   - 订单详情页面：可以更新订单状态
   - 状态按钮：只显示允许的下一步状态

3. **权限控制**：
   - 普通员工：只能查看
   - 仓库管理员：可以更新状态
   - 超级管理员：所有权限

---

### 问题 2: 是否需要两个数据库？

**答案**：不需要两个物理数据库服务器，但在同一个阿里云 RDS 上创建两个数据库

#### 数据库配置方案（推荐）⭐

```
阿里云 RDS MySQL (同一个 RDS 实例)
│
├── 开发环境 (.env)
│   ├── 数据库：warehouse_test_data
│   ├── 数据：测试数据（种子数据）
│   └── 目的：开发和测试功能
│
└── 生产环境 (.env.production)
    ├── 数据库：warehouse_product
    ├── 数据：真实业务数据
    └── 目的：实际运营
```

**优势**：
- ✅ 环境一致（都是 MySQL），无兼容性问题
- ✅ 数据隔离（测试数据不会影响生产数据）
- ✅ 可以随时重置测试数据库
- ✅ 使用同一个 RDS 实例，管理方便

#### 在 RDS 上创建两个数据库

```sql
-- 登录 RDS MySQL
mysql -h rm-xxxxx.mysql.rds.aliyuncs.com -u username -p

-- 创建测试数据库
CREATE DATABASE warehouse_test_data
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- 创建生产数据库
CREATE DATABASE warehouse_product
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- 查看数据库
SHOW DATABASES;
```

#### 环境配置文件

**开发环境 (.env)**:
```bash
DATABASE_URL=mysql+pymysql://user:pass@rm-xxxxx.mysql.rds.aliyuncs.com:3306/warehouse_test_data
DEBUG=True
LOG_LEVEL=DEBUG
```

**生产环境 (.env.production)**:
```bash
DATABASE_URL=mysql+pymysql://user:pass@rm-xxxxx.mysql.rds.aliyuncs.com:3306/warehouse_product
DEBUG=False
LOG_LEVEL=INFO
```

#### 环境切换

```bash
# 开发环境（使用 .env 文件）
cp .env.example .env
# 编辑 .env，设置 warehouse_test_data 数据库
source .venv/bin/activate
./start_backend.sh

# 生产环境（使用 .env.production 文件）
cp .env.production.example .env.production
# 编辑 .env.production，设置 warehouse_product 数据库
./deploy.sh
```

---

### 问题 3: 如何在网站上更改订单状态和数量？

**答案**：创建管理页面 + API 端点

#### 需要添加的功能

##### A. 订单管理页面 (Orders Management)

**功能**：
- 📋 订单列表（带筛选：状态、日期、经销商）
- 🔍 搜索订单（订单号、产品名）
- ✏️ 编辑订单（修改数量、状态）
- 🗑️ 取消订单
- 📊 订单详情（显示完整信息）

**界面示例**：
```
┌─────────────────────────────────────────────┐
│ 订单管理                          [+ 新建订单] │
├─────────────────────────────────────────────┤
│ 筛选: [状态: 全部 ▼] [日期范围] [搜索...]    │
├─────────────────────────────────────────────┤
│ 订单号      | 产品    | 数量 | 状态    | 操作 │
│ WH-2024-001 | 活塞组  | 100  | pending | [编辑]│
│ WH-2024-002 | 机油    | 50   | shipped | [查看]│
└─────────────────────────────────────────────┘
```

##### B. 库存管理页面 (Inventory Management)

**功能**：
- 📦 库存列表（按仓库、产品）
- ➕ 入库操作
- ➖ 出库操作
- 🔄 调整库存
- 📈 库存历史

**界面示例**：
```
┌─────────────────────────────────────────────┐
│ 库存管理                          [+ 库存调整] │
├─────────────────────────────────────────────┤
│ 产品      | 仓库   | 可用 | 预留 | 操作       │
│ 活塞组    | 主仓库 | 150  | 20   | [调整][历史]│
│ 机油 5W30 | 主仓库 | 500  | 0    | [调整][历史]│
└─────────────────────────────────────────────┘
```

---

## 🔧 实现步骤

### 步骤 1: 更新数据库模型

添加必要字段（已在 DATABASE_SCHEMA.md 中设计）

### 步骤 2: 创建后端 API

#### 订单管理 API

```python
# 更新订单状态
PUT /api/v1/orders/{order_id}/status
{
  "status": "processing",
  "notes": "开始处理订单"
}

# 更新订单数量
PUT /api/v1/orders/{order_id}
{
  "quantity": 150,
  "notes": "客户要求增加数量"
}

# 获取订单历史
GET /api/v1/orders/{order_id}/history
```

#### 库存管理 API

```python
# 调整库存
POST /api/v1/inventory/adjust
{
  "product_id": 1,
  "warehouse_id": 1,
  "quantity": 100,
  "transaction_type": "IN",  # IN, OUT, ADJUST
  "notes": "新到货"
}

# 获取库存历史
GET /api/v1/inventory/{product_id}/transactions
```

### 步骤 3: 创建前端管理页面

1. **订单管理页面** (`src/pages/OrderManagement.tsx`)
2. **库存管理页面** (`src/pages/InventoryManagement.tsx`)
3. **添加到路由** (`src/App.tsx`)

### 步骤 4: 权限控制

```python
# 装饰器示例
@router.put("/orders/{order_id}/status")
async def update_order_status(
    order_id: int,
    status_update: OrderStatusUpdate,
    current_user: User = Depends(get_current_active_user),
    db: Session = Depends(get_db)
):
    # 检查权限
    if not current_user.role in ["manager", "admin"]:
        raise HTTPException(403, "无权限")
    
    # 更新订单状态
    order = db.query(SalesOrder).filter(SalesOrder.id == order_id).first()
    if not order:
        raise HTTPException(404, "订单不存在")
    
    order.status = status_update.status
    # 记录活动日志
    log = ActivityLog(
        activity_type="order",
        action=f"订单状态更新为 {status_update.status}",
        item_name=order.order_code,
        user_id=current_user.id
    )
    db.add(log)
    db.commit()
    
    return order
```

### 步骤 5: 种子数据（开发用）

创建脚本生成测试数据：

```python
# scripts/seed_data.py
def create_seed_data(db: Session):
    """创建测试数据"""
    
    # 1. 创建用户
    admin = User(
        username="admin",
        email="admin@warehouse.com",
        phone="13800138000",
        hashed_password=get_password_hash("admin123"),
        role="admin"
    )
    
    # 2. 创建产品分类
    category = ProductCategory(
        name="发动机零件",
        code="ENGINE_PARTS"
    )
    
    # 3. 创建产品
    product = Product(
        name="6BT 活塞组",
        sku="PISTON-6BT-001",
        part_number="3803682",
        engine_model="6BT5.9",
        price=850.00,
        category_id=category.id
    )
    
    # 4. 创建仓库
    warehouse = Warehouse(
        name="主仓库",
        code="WH001",
        capacity=1000.0
    )
    
    # 5. 创建库存
    inventory = Inventory(
        product_id=product.id,
        warehouse_id=warehouse.id,
        quantity=100
    )
    
    db.add_all([admin, category, product, warehouse, inventory])
    db.commit()
```

---

## 📝 建议的实现顺序

### 阶段 1: 数据库和基础 API（优先）
1. ✅ 更新数据库模型（添加新字段）
2. ✅ 创建 ActivityLog 模型
3. ✅ 创建订单管理 API
4. ✅ 创建库存管理 API
5. ✅ 创建 Dashboard API

### 阶段 2: 前端界面
6. ✅ 更新 Dashboard（调用真实 API）
7. ✅ 创建订单管理页面
8. ✅ 创建库存管理页面
9. ✅ 添加权限控制

### 阶段 3: 测试和优化
10. ✅ 创建种子数据脚本
11. ✅ 测试完整流程
12. ✅ 优化用户体验

---

## 🎯 用户角色和权限

| 角色 | 查看 | 创建订单 | 更新订单状态 | 调整库存 | 用户管理 |
|------|------|----------|--------------|----------|----------|
| **员工 (staff)** | ✅ | ✅ | ❌ | ❌ | ❌ |
| **仓库管理员 (manager)** | ✅ | ✅ | ✅ | ✅ | ❌ |
| **系统管理员 (admin)** | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 💾 数据备份策略

**开发环境**：
- SQLite 文件定期备份
- 可以随时重置并重新生成种子数据

**生产环境**：
- 阿里云 RDS 自动备份（每日）
- 手动备份（重要操作前）
- 导出重要数据到 Excel

---

**最后更新**: 2025-11-10
